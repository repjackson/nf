template(name='cart')
    with cart_order
        .ui.stackable.padded.grid
            .sixteen.wide.column
                a.ui.header(href="/cart")
                    +i name='shopping-cart'
                    |cart
                .ui.vertical.fluid.menu.scrolli
                    each cart_items
                        +cart_item
            .sixteen.wide.column
                // a.ui.fluid.basic.icon.button(href="/shop")
                //     // i.chevron.left.icon
                //     +i name='food-cart'
                if cart_items.length
                    .ui.header subtotal: $#{my_cart_subtotal}
                    .ui.big.fluid.button.checkout_cart
                        i.large.send.icon
                        |checkout
                    
                
                

template(name='cart_item')
    .item
        // .image
        img.ui.tiny.inline.rounded.image(src="{{c.url image_id width=400 height=300 crop='pad'}}")
        .content
            a.ui.inline.header(href="/product/#{product._id}")
                small $#{product_price} x
                    | #{amount}
                |#{product_title}
            small added #{when}
            .ui.basic.icon.button.decrease_amount
                i.minus.icon
            .ui.basic.icon.button.increase_amount
                i.plus.icon
            .ui.basic.icon.button.remove_item
                i.remove.red.icon
            if is_admin
                +print_this
