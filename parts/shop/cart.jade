template(name='cart')
    .ui.stackable.padded.grid
        .four.wide.column
            .ui.header
                i.cart.icon
                |cart
            a.ui.fluid.circular.basic.icon.button(href="/shop")
                i.shop.large.icon
        .twelve.wide.column
            .ui.large.list.scrolling
                each cart_items
                    .item
                        img.ui.image(src="{{c.url product.image_id width=400 height=300 crop='pad'}}")
                    
                        .ui.header $#{product.price_usd} #{product.title}
                        small added #{when}
                        .ui.circular.basic.icon.button.remove_item
                            i.remove.red.icon
                        +print_this
            .ui.header subtotal: $#{my_cart_subtotal}
            